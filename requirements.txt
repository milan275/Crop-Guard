# ── Bootstrap (required before anything else in fresh venvs) ─────────────────
setuptools>=70.0.0
wheel>=0.43.0

# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
python-dotenv==1.0.1
pydantic==2.7.1
pydantic-settings==2.2.1
email-validator==2.1.1

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy==2.0.30
aiosqlite==0.20.0

# ── Geospatial (use newer versions that have Python 3.12/3.13 wheels) ─────────
geopandas>=0.14.4
shapely>=2.0.4
rasterio>=1.3.10
pyproj>=3.6.1
fiona>=1.9.6

# ── Scientific / ML ───────────────────────────────────────────────────────────
numpy>=1.26.4
pandas>=2.2.2
scikit-learn>=1.4.2
scipy>=1.13.0

# TensorFlow: use tensorflow-cpu for lighter install; swap for tensorflow on GPU
# tensorflow==2.16.1 may not have Python 3.13 wheels yet — use 2.17+ or cpu build
tensorflow-cpu>=2.16.1

# ── Satellite / STAC ──────────────────────────────────────────────────────────
pystac-client>=0.7.7
planetary-computer>=1.0.0
stackstac>=0.5.1

# ── HTTP ──────────────────────────────────────────────────────────────────────
httpx>=0.27.0
requests>=2.31.0

# ── Data ──────────────────────────────────────────────────────────────────────
xarray>=2024.3.0
dask[array]>=2024.5.0
netCDF4>=1.6.5

# ── Visualisation (pipeline / scripts only) ──────────────────────────────────
matplotlib>=3.8.4
pillow>=10.3.0

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.66.4
python-dateutil>=2.9.0.post0
pytz>=2024.1
joblib>=1.4.2

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.7
anyio>=4.3.0
